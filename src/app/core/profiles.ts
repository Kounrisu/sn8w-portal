/**
 * Philippe's public profiles, in one place: the hero card and the footer
 * both link to them, and a URL that lives in two templates is a URL that
 * eventually disagrees with itself.
 *
 * Brand names are not translated, so the visible labels stay in the
 * templates rather than the i18n dictionary.
 */
export const PROFILES = {
  github: 'https://github.com/Kounrisu',
  linkedIn: 'https://www.linkedin.com/in/sn8w22/',
  frontendMasters: 'https://master.dev/u/sn8w/',
} as const;
